// Client scripts (scaffold). JavaScript lives here, never inline in templates.
// Run `npm run lint` after edits.

document.addEventListener('DOMContentLoaded', () => {
  // Placeholder for interactive behaviour your project adds.
});
